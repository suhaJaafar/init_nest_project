export enum SupportedLanguages {
  English = "en",
  Arabic = "ar",
}

export enum SortOrder {
  ASC = "ASC",
  DESC = "DESC",
}

export enum HeaderAcceptOptions {
  Default = "*/*",
  JSON = "application/json",
}

export enum Weekday {
  Monday = "MONDAY",
  Tuesday = "TUESDAY",
  Wednesday = "WEDNESDAY",
  Thursday = "THURSDAY",
  Friday = "FRIDAY",
  Saturday = "SATURDAY",
  Sunday = "SUNDAY",
}

export enum ValidationPropertyPlace {
  Body = "BODY",
  Params = "PARAMS",
  Query = "QUERY",
  Headers = "HEADERS",
}

export enum NotificationChannel {
  Email = "EMAIL",
  SMS = "SMS",
  Push = "PUSH",
}
